package fi.validi.network.model {
	import fi.validi.network.model.model2d.Node2D;
	import flash.events.Event;
	import flash.events.EventDispatcher;
	/**
	 * @author Juho Viitasalo
	 */
	public class NetWorld extends AbstractNetWorld {
		
		public function NetWorld() {
			super();
		}
		
//		public function createNode() : INode {
//			var networkVector : Vector.<INetwork> = new Vector.<INetwork>();
//			networkVector.push(createNetwork());
//			_createdNode = new Node(networkVector);
//			
//			_nodes.push(_createdNode);
//			dispatchEvent(new Event(NetWorldEvent.NODE_CREATED));
//			return _createdNode;
//		}
	}
}
